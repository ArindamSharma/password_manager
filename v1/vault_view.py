# import tkinter as tk
# from main import *
# vault_view={}
# vault_view["root"]=tk.Frame(master["root"],)
# vault_view["root"].grid(row=0,column=0,sticky=tk.N+tk.E+tk.W+tk.S,)

# vault_view["head"]={}
# vault_view["head"]["root"]=tk.Frame(vault_view["root"],bg="blue")
# vault_view["head"]["root"].pack(side=tk.TOP,fill=tk.X)

# vault_view["body"]={} 
# vault_view["body"]["root"]=tk.Frame(vault_view["root"],bg="red")
# vault_view["body"]["root"].pack(side=tk.BOTTOM,fill=tk.BOTH,expand=True)
# master["view"]["vault_view"]=vault_view